/* ---------------------------------------------------------
   config.js — points the static frontend (Vercel) at the
   Flask backend (Render). Change this one line if the
   Render URL ever changes.
--------------------------------------------------------- */
const API_BASE = "https://bangalore-house-price-prediction-2xdu.onrender.com";
